{
  "name": "chatapp-server",
  "version": "1.0.0",
  "description": "chat application server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Julia Espinoza",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^6.0.0",
    "socket.io": "^2.1.1"
  }
}
